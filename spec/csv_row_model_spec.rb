require 'spec_helper'

describe CsvRowModel do
  it 'has a version number' do
    expect(CsvRowModel::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
